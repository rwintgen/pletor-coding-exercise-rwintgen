# Project Journal

## Task Log

### Initial Setup
- Created `.github/copilot-instructions.md` with workflow rules and coding standards
- Created `format.sh` using ESLint (frontend) and Ruff (backend)
- Scaffolded frontend structure: features/, lib/, stores/, components/ui/
- Created `theme.ts` with design tokens
- Created `/docs` folder with architecture documentation
- Chose feature-based folder structure for scalability
- Commit: `0d41ddc` — chore: scaffold project structure, theme, tooling, and docs

### Fix format.sh
- Added `npm install` before eslint (deps weren't installed)
- Rewrote to use `docker compose exec backend ruff` for Python formatting
- Fixed `services/__init__.py` that had TypeScript syntax
- Commit: `524cf7e` — fix: format script uses docker for backend, fix python init file and format all code

### Git remote switch
- Removed origin `Lasqo/pletor-coding-exercise` → added `rwintgen/pletor-coding-exercise-rwintgen`
- (included in commit above)

### Start app with Docker
- Launched Docker Desktop
- Fixed `no such column: images.file_size` by adding `drop_all` before `create_all` in lifespan
- (fix included in Phase 1 commit)

### Trim copilot-instructions
- Removed tech stack and project structure sections (already in docs)
- Kept only actionable workflow + code rules
- Commit: `ae06156` — docs: trim copilot-instructions, update journal

### Phase 1 — Authentication & Ownership
- Created `User` model (id, username, hashed_password, created_at)
- Added `user_id` FK to `Image` model (replaces plain `user` string)
- Created `auth.py`: bcrypt hashing, JWT (HS256, 24h), `get_current_user` dependency
- Created `routers/auth.py`: POST /register, POST /login, GET /me
- Protected upload (requires auth), delete (requires auth + ownership)
- Updated seed to create users then link images via FK
- Fixed bcrypt/passlib compat (pinned bcrypt==4.0.1)
- Chose JWT Bearer over cookies for SPA simplicity
- Seeded users all have password "password"
- Commit: `2f319cd` — feat: add authentication, user model, and ownership-based access control

### Docs + Tests
- Updated docs/architecture.md with DB schema, API table
- Wrote 19 pytest tests (auth + images endpoints + edge cases)
- All pass against in-memory SQLite

### Phase 2 — Upload Quotas
- Created `services/quota.py`: per-user (10/day) and global (100/day) limits
- `enforce_quota()` checks counts via `SELECT COUNT` filtered by today's start (UTC midnight)
- Returns 429 with clear message when limit hit
- Added `GET /images/quota` endpoint returning full status (remaining, limits, can_upload)
- Quota checked before file write (fail-fast)
- Race conditions: SQLite serializes writes inherently; noted Postgres strategy in docs
- 8 new tests: unauthenticated, fresh user, decrement, user exceeded, status reflects exceeded, global exceeded (patched limit), independent per-user, midnight reset
- All 27 tests pass
- Commit: `084230b` — feat: implement upload quotas (10/user/day, 100 global/day) with status endpoint and tests

### Restructure docs
- Split monolithic architecture.md into 3 files:
  - docs/README.md — how to run, test, format
  - docs/architecture.md — system design, API endpoints, auth flow
  - docs/schema.md — DB tables, relationships, seed data
- Commit: `36421a5` — docs: split docs into README, architecture, and schema files; add tests

### Deep Audit
- App.tsx: replaced all hardcoded inline styles with theme.ts tokens (colors, spacing, typography, radii, shadows, transitions)
- App.tsx: removed stale `user` state + form field, fixed `img.user` → `img.username`
- api/client.ts: already had JSDoc from earlier pass
- schemas.py: added `min_length=1` validation on username and password
- Added 4 new tests: empty username, empty password, upload missing title, delete frees quota slot
- Total: 31 tests, all passing
- Docs verified accurate
- Commit: `45dcb71` — refactor: deep audit — theme tokens, validation, and 4 new tests

### Phase 3 + 4 — Frontend auth & polished gallery
- Installed `zustand`, `@tanstack/react-query`, `react-router-dom`, `zod`
- `stores/auth.ts`: Zustand store with `persist` middleware (token + user)
- Updated `api/client.ts` to read token from auth store; `login()` no longer touches localStorage directly
- `lib/schemas.ts`: Zod schemas for login, register, upload
- `lib/queryClient.ts`: TanStack Query client (30s staleTime, no refetch-on-focus)
- `hooks/useImages.ts`, `hooks/useQuota.ts`: query + mutation hooks; mutations invalidate cache
- UI primitives: `Button` (4 variants × 3 sizes), `Input` (label+error), `Card` (hover-lift), `Spinner`
- Composed components: `Navbar`, `ImageCard` (owner-only delete, hover zoom), `DropZone`, `QuotaIndicator` (color-coded progress), `Modal` (Escape + backdrop close), `EmptyState`, `ErrorBanner`
- Pages: `LoginPage` + `RegisterPage` (shared `AuthShell`), `GalleryPage` (sidebar upload + grid), `UploadForm`, `ImageDetail` (lightbox)
- `App.tsx`: react-router with `/`, `/login`, `/register`; revalidates persisted token via `/auth/me` on mount; clears auth on 401
- `index.css`: keyframes (spin, fadeIn, scaleIn) + base resets
- Removed legacy `App.css` and old monolithic `App.tsx`
- Build + lint clean (351 KB JS, 108 KB gzipped)
- Skipped Shadcn/Radix to keep things simple per company guidance ("simplicité et efficacité")
- Commit: `7866c58` — feat: frontend auth + polished gallery (phases 3 & 4)

### Search, Sort, User Profiles
- Backend: added `?search`, `?user`, `?sort` query params to `GET /images/`
  - `search`: case-insensitive ILIKE on title
  - `user`: exact username filter
  - `sort`: `newest` (default), `oldest`, `title`
- Backend: new `routers/users.py` with `GET /users/{username}` (profile) and `GET /users/{username}/images`
- Frontend: `GalleryToolbar` component — search input + sort dropdown
- Frontend: `ProfilePage` at `/user/:username` — shows user info + their images; owner can delete
- Frontend: username is clickable in both `ImageCard` and `ImageDetail` lightbox → links to profile
- Frontend: Navbar "Signed in as X" links to own profile
- 11 new backend tests (search, case-insensitive, filter by user, sort oldest, sort title, no results, user profile, user not found, user images, empty, not found)
- Total: 42 tests, all passing
- Docs updated: API table, routing table, component list
- Commit: `8e38860` — feat: search/sort toolbar, user profiles, username links + 11 new tests

### Bug fixes + polish
- **Profile routing**: replaced `<Link>` with `useNavigate()` for username clicks in both ImageCard and ImageDetail — prevents event being swallowed by Card/Modal ancestors. Lightbox `onClose` fires before navigate.
- **Username hover**: added underline on hover for all username links.
- **theme.ts trimmed**: removed ~60% of unused tokens (neutral 400, primary 100–400/700–900, success 50/700, warning 50/700, radii.sm, shadows.lg, transitions.slow, typography.lineHeight, typography.fontWeight.normal, spacing.4xl, fontSize.2xl—only used where needed).
- **Toolbar redesign**: added filter-by-user dropdown alongside sort; custom chevron arrow SVG on selects; consistent padding and alignment.
- **Upload UX**: removed sidebar panel, replaced with FAB (floating "+" button, bottom-right) that opens a modal overlay with the upload form.
- **Broken images**: seed URLs now include `?w=800&fit=crop&auto=format` for reliable Unsplash delivery.
- **Scalability**: added DB indexes on `title`, `created_at`, `user_id`; added `limit`/`offset` pagination to `GET /images/` (default 50, max 200). Documented next steps for millions of images (full-text search, cursor pagination, CDN, read replicas).
- 1 new test: `test_pagination_limit_offset`
- Total: 43 tests, all passing
### Modern frontend redesign (high-end visual polish)
- **Design system overhaul** in `theme.ts`: indigo→violet→rose brand gradient, expanded neutral scale (0–950 incl. 150 step), 7-tier shadow ladder (xs→xl + glow + glowAccent + brand), cubic-bezier easing tokens, generous radii, Inter via Google Fonts.
- **Animations everywhere**: page-enter slide-up, staggered card fade-in (`.stagger-fade` with nth-child delays), modal scale-in, shimmer placeholders, hover lifts (`translateY(-4px)` cards, `translateY(-1px)` buttons, scale on FAB), gradient overlay reveal on image hover.
- **Glass effects**: navbar uses `backdrop-filter: blur` over translucent surface; modal backdrop blurs page content.
- **UI primitives rebuilt**: Button (gradient brand variant + scale-on-press), Input (animated focus glow ring), Card (lift + shadow on hover), Spinner, Modal, DropZone, EmptyState (icon circle), ErrorBanner (slide-down).
- **New `Fab` component** shared by Gallery + Profile pages.
- **ImageCard**: shimmer skeleton until loaded, hover zoom + gradient overlay, animated delete button (opacity + scale).
- **GalleryToolbar**: SVG search icon inside input, focus glow ring, custom select chevron.
- **Auth pages**: redesigned AuthShell with stronger card shadow, gradient title, brand-tinted radial backdrop.
- Commit: `2e96c95` — feat(ui): modern redesign — gradient brand, smooth animations, glass effects, refined tokens

### Final bonus pass — masonry, batch upload, dark mode
- **Masonry layout**: replaced fixed CSS grid with CSS multi-column (`column-count` 4→3→2→1 responsive). Cards now flow naturally and `break-inside: avoid` keeps them intact. Removed forced 4:3 aspect-ratio in `ImageCard` so each image displays at its real proportion. Shimmer placeholder still works via `min-height` until load.
- **Batch upload**: `DropZone` now accepts `multiple` files (drop or browse). `UploadForm` uploads sequentially, shows `Uploading 3/5…` progress, falls back to filename-as-title when shared title is empty, and pre-flight-checks the selection against remaining daily quota (refuses to start if it would exceed).
- **Dark mode (auto, follows system)**: introduced full CSS variable palette in `index.css` (`--neutral-0..950`, `--primary-50..700`, `--accent-400..600`, `--bg-page`, `--surface-glass`, `--scrollbar-thumb`, etc.). `theme.ts` `colors.neutral/primary/accent` now resolve to `var(--…)` so existing inline styles automatically swap. `@media (prefers-color-scheme: dark)` overrides every var with an inverted palette. Navbar glass and Auth shell radial backdrop use vars too. No flash, no toggle, no app-wide refactor.
- Lint + build clean (4.37 KB CSS, 366 KB JS).

### Final audit pass (interview polish)
Ran a structured audit across components/theming, docs, architecture, tests, security, a11y, build hygiene. Acted on the high-impact findings:

**Backend**
- New `app/config.py`: single source of truth for `JWT_SECRET`, `JWT_ALGORITHM`, `JWT_EXPIRE_MINUTES`, `CORS_ORIGINS`, `MAX_UPLOAD_BYTES`, `USER_DAILY_LIMIT`, `GLOBAL_DAILY_LIMIT`. All read from env vars with sane defaults; auth/main/quota/images now import from it. Production deployments override via env without code changes.
- Removed hardcoded `SECRET_KEY = "dev-secret-change-in-production"` from `auth.py`.
- Added `MAX_UPLOAD_BYTES` enforcement in upload route → 413 on oversized files. Defaults to 10 MB.
- Extracted `_image_to_response` (was duplicated in `routers/images.py` and `routers/users.py`) into `schemas.image_to_dict`. Both routers now share one helper.
- Added `_get_user_or_404` helper in `routers/users.py` (was duplicated lookup logic).
- Docstrings added to every endpoint and to `get_current_user`, `image_to_dict`, `_today_start`.

**Frontend**
- New `tsconfig.json` with `strict: true`, `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`. Added `tsc --noEmit` as `npm run typecheck`. Codebase passes strict mode unchanged — confirms type quality was already solid.
- New `vite-env.d.ts` typing `import.meta.env.VITE_API_URL`.
- `api/client.ts`: `API_URL` now reads from `VITE_API_URL` env var, fallback to localhost.
- New `lib/errors.ts` with typed `errorMessage(unknown)` helper. Replaced all `catch (err: any) { setError(err.message) }` patterns in `LoginPage`, `RegisterPage`, `UploadForm` with `catch (err) { setError(errorMessage(err)) }`.
- `ProfilePage`: account-deletion was silently swallowing errors → now surfaces them via an `ErrorBanner`.
- `App.tsx`: added comment to the intentional empty `.catch()` (token expired → clear auth) so intent is explicit.
- A11y: added `aria-hidden` + `focusable="false"` to decorative search SVG in `GalleryToolbar`.

**Tests (+4 → 47 total)**
- `test_upload_oversized_file_rejected` — verifies 413 on payloads larger than `MAX_UPLOAD_BYTES`.
- `test_delete_image_is_idempotent` — first delete → 204, second delete → 404; safe to retry.
- `test_search_param_does_not_leak_sql` — proves SQLAlchemy parameterizes user input (`' OR '1'='1` returns no rows).
- `test_expired_token_rejected` — manually mints an expired JWT with the live secret and confirms 401.

**Repo hygiene**
- New `.env.example` documenting every env var (backend + `VITE_API_URL`).
- Expanded `.gitignore` (was 4 lines): now covers `.env*`, `.DS_Store`, `__pycache__`, `dist/`, `backend/test.db`, `backend/uploads/`, etc.
- Untracked `backend/test.db` (regenerates on startup via `seed_if_empty`).

**Docs**
- `docs/architecture.md`: added `config.py` and `services/quota.py` to backend file list, added `DELETE /auth/account` to the API table, documented `image_to_dict` shared helper, fixed `search` description (also matches usernames), fixed status code list on upload (400 / 413 / 429), documented `delete` idempotency.

**Verification**
- `tsc --noEmit` clean. `eslint .` clean. `vite build` clean (4.4 KB CSS, 367 KB JS). `pytest` 47/47 pass.

Result targets the 8.5/10 ceiling identified in the audit: env-driven config, file-size enforcement, typed error handling, strict TypeScript, expanded edge-case tests, and a clean repo.

### UX polish: ConfirmDialog, navbar trim, cache invalidation
- **ConfirmDialog component** (`components/ConfirmDialog.tsx`): reusable themed confirmation popup with glass backdrop, `fadeIn`/`scaleIn` animations, Escape + backdrop-click dismiss, `danger`/`primary` variants, `loading` prop for async actions. Uses existing `Button` (danger + ghost) and `Modal`-style backdrop.
- Replaced all native `window.confirm()` calls with `ConfirmDialog`:
  - `GalleryPage`: image delete via `deleteTarget` state
  - `ProfilePage`: image delete (same pattern) + **account delete** (previously had zero confirmation)
- **Navbar username trim**: usernames > 15 characters are trimmed with ellipsis (`…`) in the nav bar.
- **Cache invalidation on account delete**: added `useQueryClient` in `ProfilePage`; after `deleteAccount()`, calls `queryClient.removeQueries` on `['images']` and `['user', username]` so the gallery refreshes immediately instead of showing stale cards.
- Commit: `d52e0b9` — feat: ConfirmDialog component, navbar username trim, cache invalidation on account delete

### Docs accuracy audit + seed data overhaul
- **Docs fixes**:
  - `schema.md`: "from Unsplash" → "from Lorem Picsum (picsum.photos)"; updated seed description (2-year spread, mixed aspect ratios, duplicates).
  - `architecture.md`: corrected "cascade delete on user → images" — cascade is manual in `routers/auth.py::delete_account`, not via SQLAlchemy `relationship(cascade=…)`. Added `ConfirmDialog` to component list, `lib/errors.ts` to frontend list, full keyframes list (`fadeIn`, `scaleIn`, `slideUp`, `slideDown`, `shimmer`, `pulse`, `spin`), note about `drop_all`+`create_all` on startup (dev-only), `users.username` index.
  - `README.md`: replaced stale "auto-fill grid" line with masonry; added `ConfirmDialog` to polished gallery checklist.
- **Seed data rewrite** (`seed.py`):
  - 12 varied aspect ratios cycled deterministically: 3:2, 2:3, 1:1, 16:9, 9:16, 2:1 panoramic, 3:4, 3:2 hi-res, 2:3 mid-res, 4:3, 3:4, 4:5 social.
  - 55 timestamps spread across ~2 years (today → 731 days ago) with uneven spacing and several deliberate `created_at` duplicates to exercise stable sort.
- Commit: `fdc75b0` — docs: fix inaccuracies, add missing items; seed: varied aspect ratios + 2-year date spread

### Global quota bar, image rename, UI fixes
- **Global quota progress bar**: `QuotaIndicator` now shows two bars — "Your uploads" (user quota, 8px) and "Team uploads" (global quota, 6px) — both with the same color-coded thresholds.
- **Image rename (PATCH /images/{id})**:
  - Backend: new `ImageUpdate` Pydantic schema (`title` min 1, max 200), new `PATCH /{image_id}` endpoint in `routers/images.py` (owner-only, 403/404). Docs updated with PATCH in API table.
  - Frontend: `renameImage()` in `api/client.ts`, `useRenameImage` mutation hook in `hooks/useImages.ts`, inline title editing in `ImageDetail` lightbox (click title → input + checkmark save + X cancel buttons, Escape to cancel).
  - 4 new backend tests: rename own, rename forbidden (other user), rename requires auth, rename empty title rejected. Total: **51 tests**, all passing.
- **Avatar shape**: navbar avatar changed from circle (`borderRadius: 50%`) to square with rounded corners (`borderRadius: radii.lg`).
- **Delete button icon**: replaced Unicode `×` with SVG X icon in `ImageCard`; button shape changed from circle to rounded-corner square (`borderRadius: radii.lg`).
- **FAB (upload button)**: replaced text `+` with SVG plus icon; removed `translateY(-3px)` from hover (was causing upward drift) → now uses `scale(1.08)` centered via `display: flex; align-items: center; justify-content: center`. Corner radius aligned with other icons (`radii.lg`).

### Optimistic rename + navbar polish
- **Optimistic title update**: `ImageDetail` now tracks a `localTitle` state that updates instantly on save; syncs back from the `image` prop via `useEffect` when query refetches.
- **Navbar floating pill**: changed from full-bleed `borderBottom` bar to a floating pill with rounded corners (`radii.xl`), `top: spacing.sm` offset and `margin: 0 spacing.sm` so page content peeks above and on the sides.
- **Profile link hover**: the avatar + username `<Link>` now has padding + `borderRadius: radii.lg` and a subtle `colors.neutral[100]` background on hover, matching the ghost button feel of "Sign out".

### Masonry L→R fix, theme toggle, UI polish
- **Masonry reading order fix**: replaced CSS `column-count` masonry (which fills top-to-bottom per column) with a JS flex-column approach (`MasonryGrid` component). Images are distributed round-robin into N vertical flex columns (`item[i] → column[i % cols]`), ensuring left-to-right reading order. Responsive via `matchMedia` listeners matching the same breakpoints. Applied to both `GalleryPage` and `ProfilePage`; old `.masonry` CSS rules removed.
- **Theme toggle**: new Zustand store (`stores/theme.ts`) with `system | light | dark` modes, persisted to localStorage. Navbar shows a cycle button (computer → sun → moon SVG icons). CSS updated: `prefers-color-scheme: dark` now respects `data-theme` attribute override — `[data-theme="light"]` forces light, `[data-theme="dark"]` forces dark, no attribute = system default.
- **Danger zone dark mode**: error color tokens (`error.50/500/600/700`) converted from hard-coded hex to CSS variables with dark-mode variants (muted red background, lighter text).
- **Blurred lightbox background**: portrait/narrow images in the `ImageDetail` modal now show a blurred + dimmed version of the same image as background (`filter: blur(30px) brightness(0.6)`) instead of flat black/white bands.
- **UI tweaks**: FAB corners rounder (`radii.lg` → `radii.xl`), profile avatar changed from circle to rounded square (`radii.full` → `radii.lg`), EmptyState emoji background same change.
- **Full-height pages**: added `minHeight: '100vh'` to `GalleryPage` and `ProfilePage` so the body radial gradient always reaches the viewport bottom.

### Navbar logged-out polish + Button component consolidation
- **Sign in link**: replaced text-color-change hover with background-highlight hover (`neutral[100]`), matching the theme toggle button style. Now styled as a pill (36px height, `radii.lg`).
- **Sign up button**: new `glimmer` variant added to `Button`. At rest it looks like `primary` (brand gradient, white text). On hover the gradient is stretched to 200% width and the `glimmer` keyframe sweeps `background-position` at 0.1s (fast shimmer). No `translateY` lift — stays in place.
- **Button `icon` variant**: new variant for small square icon-only buttons — transparent bg, `neutral[600]` color, `neutral[100]` bg on hover, `radii.lg`. Added `square` prop (forces `width = height` based on size).
- **Converted raw `<button>` elements**:
  - Navbar theme toggle → `<Button variant="icon" size="sm" square>`
  - ImageDetail save/cancel icon buttons → `<Button variant="icon" size="sm" square>` with custom background/color via `style`
- **Not converted** (too specialized for the Button API):
  - `Fab.tsx` — fixed-position FAB with scale animation
  - `ImageCard.tsx` delete — absolute-positioned overlay with backdrop blur
  - `ErrorBanner.tsx` dismiss — minimal inline "×" character
- Button variant type now: `primary | secondary | danger | ghost | glimmer | icon`

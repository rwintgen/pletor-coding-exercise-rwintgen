# Project Journal

## Task Log

### Initial Setup
- Created `.github/copilot-instructions.md` with workflow rules and coding standards
- Created `format.sh` using ESLint (frontend) and Ruff (backend)
- Scaffolded frontend structure: features/, lib/, stores/, components/ui/
- Created `theme.ts` with design tokens
- Created `/docs` folder with architecture documentation
- Chose feature-based folder structure for scalability
- Commit: `0d41ddc` — chore: scaffold project structure, theme, tooling, and docs

### Fix format.sh
- Added `npm install` before eslint (deps weren't installed)
- Rewrote to use `docker compose exec backend ruff` for Python formatting
- Fixed `services/__init__.py` that had TypeScript syntax
- Commit: `524cf7e` — fix: format script uses docker for backend, fix python init file and format all code

### Git remote switch
- Removed origin `Lasqo/pletor-coding-exercise` → added `rwintgen/pletor-coding-exercise-rwintgen`
- (included in commit above)

### Start app with Docker
- Launched Docker Desktop
- Fixed `no such column: images.file_size` by adding `drop_all` before `create_all` in lifespan
- (fix included in Phase 1 commit)

### Trim copilot-instructions
- Removed tech stack and project structure sections (already in docs)
- Kept only actionable workflow + code rules
- Commit: `ae06156` — docs: trim copilot-instructions, update journal

### Phase 1 — Authentication & Ownership
- Created `User` model (id, username, hashed_password, created_at)
- Added `user_id` FK to `Image` model (replaces plain `user` string)
- Created `auth.py`: bcrypt hashing, JWT (HS256, 24h), `get_current_user` dependency
- Created `routers/auth.py`: POST /register, POST /login, GET /me
- Protected upload (requires auth), delete (requires auth + ownership)
- Updated seed to create users then link images via FK
- Fixed bcrypt/passlib compat (pinned bcrypt==4.0.1)
- Chose JWT Bearer over cookies for SPA simplicity
- Seeded users all have password "password"
- Commit: `2f319cd` — feat: add authentication, user model, and ownership-based access control

### Docs + Tests
- Updated docs/architecture.md with DB schema, API table
- Wrote 19 pytest tests (auth + images endpoints + edge cases)
- All pass against in-memory SQLite

### Phase 2 — Upload Quotas
- Created `services/quota.py`: per-user (10/day) and global (100/day) limits
- `enforce_quota()` checks counts via `SELECT COUNT` filtered by today's start (UTC midnight)
- Returns 429 with clear message when limit hit
- Added `GET /images/quota` endpoint returning full status (remaining, limits, can_upload)
- Quota checked before file write (fail-fast)
- Race conditions: SQLite serializes writes inherently; noted Postgres strategy in docs
- 8 new tests: unauthenticated, fresh user, decrement, user exceeded, status reflects exceeded, global exceeded (patched limit), independent per-user, midnight reset
- All 27 tests pass
- Commit: `084230b` — feat: implement upload quotas (10/user/day, 100 global/day) with status endpoint and tests

### Restructure docs
- Split monolithic architecture.md into 3 files:
  - docs/README.md — how to run, test, format
  - docs/architecture.md — system design, API endpoints, auth flow
  - docs/schema.md — DB tables, relationships, seed data
- Commit: `36421a5` — docs: split docs into README, architecture, and schema files; add tests

### Deep Audit
- App.tsx: replaced all hardcoded inline styles with theme.ts tokens (colors, spacing, typography, radii, shadows, transitions)
- App.tsx: removed stale `user` state + form field, fixed `img.user` → `img.username`
- api/client.ts: already had JSDoc from earlier pass
- schemas.py: added `min_length=1` validation on username and password
- Added 4 new tests: empty username, empty password, upload missing title, delete frees quota slot
- Total: 31 tests, all passing
- Docs verified accurate
- Commit: `45dcb71` — refactor: deep audit — theme tokens, validation, and 4 new tests

### Phase 3 + 4 — Frontend auth & polished gallery
- Installed `zustand`, `@tanstack/react-query`, `react-router-dom`, `zod`
- `stores/auth.ts`: Zustand store with `persist` middleware (token + user)
- Updated `api/client.ts` to read token from auth store; `login()` no longer touches localStorage directly
- `lib/schemas.ts`: Zod schemas for login, register, upload
- `lib/queryClient.ts`: TanStack Query client (30s staleTime, no refetch-on-focus)
- `hooks/useImages.ts`, `hooks/useQuota.ts`: query + mutation hooks; mutations invalidate cache
- UI primitives: `Button` (4 variants × 3 sizes), `Input` (label+error), `Card` (hover-lift), `Spinner`
- Composed components: `Navbar`, `ImageCard` (owner-only delete, hover zoom), `DropZone`, `QuotaIndicator` (color-coded progress), `Modal` (Escape + backdrop close), `EmptyState`, `ErrorBanner`
- Pages: `LoginPage` + `RegisterPage` (shared `AuthShell`), `GalleryPage` (sidebar upload + grid), `UploadForm`, `ImageDetail` (lightbox)
- `App.tsx`: react-router with `/`, `/login`, `/register`; revalidates persisted token via `/auth/me` on mount; clears auth on 401
- `index.css`: keyframes (spin, fadeIn, scaleIn) + base resets
- Removed legacy `App.css` and old monolithic `App.tsx`
- Build + lint clean (351 KB JS, 108 KB gzipped)
- Skipped Shadcn/Radix to keep things simple per company guidance ("simplicité et efficacité")
# Project Journal

## Session 1 — Initial Setup

### Actions taken
- Created `.github/copilot-instructions.md` with workflow rules and coding standards
- Created `format.sh` using ESLint (frontend) and Ruff (backend) for consistent formatting
- Scaffolded the frontend project structure: features/, lib/, stores/, components/ui/
- Created `theme.ts` with design tokens (colors, spacing, typography, radii, shadows)
- Created `/docs` folder with architecture documentation

### Reasoning
- Chose TanStack Query + Zustand + Zod + Shadcn as instructed by the company's preferred stack
- Chose feature-based folder structure (features/) to co-locate auth, gallery, and upload logic rather than flat component directories — scales better for this multi-feature task
- Used CSS custom properties in theme.ts for runtime flexibility and easy theming
- Used Ruff for backend formatting as it's the modern standard for Python (fast, replaces black + isort + flake8)

### Commits
- `0d41ddc` — chore: scaffold project structure, theme, tooling, and docs
- `524cf7e` — fix: format script uses docker for backend, fix python init file and format all code

---

## Session 1 — Task Log

### Task: Fix format.sh frontend deps (no commit — user said skip journal)
- ESLint couldn't run because node_modules weren't installed
- Added `npm install` before `npx eslint`
- Tried pip for backend but macOS blocks system Python installs
- Settled on graceful fallback message pointing to Docker

### Task: Disconnect git remote, connect new repo
- Removed origin pointing to `Lasqo/pletor-coding-exercise`
- Added new origin: `rwintgen/pletor-coding-exercise-rwintgen`
- Added conventional commit prefix rule to copilot-instructions.md
- Committed and pushed all scaffolding work

### Task: Start app with Docker
- Docker Desktop wasn't running — launched it with `open -a Docker`
- `docker compose up --build` hit `no such column: images.file_size` because old test.db lacked new columns
- Fixed by adding `drop_all` before `create_all` in lifespan handler (dev-only approach, DB is re-seeded anyway)
- Hot-reload picked up the fix, app started successfully

### Task: Fix format.sh for backend
- `services/__init__.py` had TypeScript `export {}` — replaced with empty file
- Rewrote format.sh to use `docker compose exec backend ruff` (requires running containers)
- Ran formatter, all files clean, committed and pushed

### Task: Phase 1 — Authentication & Ownership
- Created `User` model with id, username, hashed_password, created_at
- Added `user_id` FK to `Image` model (replaces plain `user` string field)
- Created `auth.py`: bcrypt hashing, JWT creation/validation, `get_current_user` dependency
- Created `routers/auth.py`: POST /auth/register, POST /auth/login, GET /auth/me
- Protected upload (requires auth), delete (requires auth + ownership check: 403 if not owner)
- Updated seed to create User records first, then link images via user_id
- Fixed bcrypt/passlib compatibility (pinned bcrypt==4.0.1)
- Updated schemas: added UserCreate, UserRead, Token; ImageRead now includes username via join
- Chose JWT in Authorization header over session cookies for simplicity and SPA compatibility
- All seeded users have password "password" for dev/testing